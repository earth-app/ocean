# Project Context: Ocean (The Earth App Algorithms)

Purpose
- Provide Claude persistent, concise context for the `ocean` frontend so it can help with development, testing, and maintenance without repeating basics each session.

Quick orientation
- Project: Kotlin Multiplatform library (JS + JVM) used by Earth App frontend components.
- Key files and folders:
  - [build.gradle.kts](build.gradle.kts)
  - [settings.gradle.kts](settings.gradle.kts)
  - [dokka.sh](dokka.sh)
  - [.github](.github)
  - [src](src)
  - Tests: [src/common/test](src/common/test) and platform tests under `jvm`/`js` source sets.

How Claude should help with this frontend
1. Start by reading `build.gradle.kts` and `src` to understand platforms (JS + JVM) and where code lives.
2. When asked to change code, follow this workflow:
   - Explore: identify affected source sets under [src](src).
   - Plan: propose a short implementation plan listing files to modify and the tests to update or add.
   - Code: make small, focused commits with explanatory commit messages.
   - Test: run `js` and `jvm` tests locally (commands below) and include expected test targets in the PR.

Common commands
```bash
# Run JVM tests
./gradlew jvmTest

# Run JS tests
./gradlew jsTest

# Run coverage report for JVM
./gradlew jvmJacocoTestReport

# Build & publish dokka docs (local script)
./dokka.sh "<short-description-or-ci-run-id>"
```

Repository integrations
- When referencing other Earth App repositories, use the following repo names (exact):
  - `earth-app/crust` — (frontend UI components / host app)
  - `earth-app/sky` — (shared UI utilities / design tokens)
  - `earth-app/cloud` — (backend services; see cloud API conventions)

Integration guidance
- Cross-repo references: when suggesting code that touches multiple repos, outline changes separately per repo and include the exact repo name and path where the change belongs.
- Releases and packaging:
  - `ocean` publishes Kotlin Multiplatform artifacts and an npm package. Follow `build.gradle.kts` publishing sections for Maven/NPM settings.
  - For npm publishes, `NPM_TOKEN` and `GITHUB_TOKEN` env vars are required in CI — never include token values in `CLAUDE.md`.
- Docs: use [dokka.sh](dokka.sh) to update the `gh-pages` branch; include what changed in the commit message.

Useful file references (workspace)
- Build config: [build.gradle.kts](build.gradle.kts)
- Multi-project setup: [settings.gradle.kts](settings.gradle.kts)
- Docs publish helper: [dokka.sh](dokka.sh)
- CI/workflows: [.github](.github) (see `workflows/build.yml` and `workflows/release.yml`)
- Source tree: [src](src) (common, jvm, js folders)
- Tests: [src/common/test](src/common/test)

Testing and verification requirements
- All changes affecting logic must include or update unit tests under the appropriate source set (common/jvm/js).
- Prefer TDD for algorithmic work: propose failing tests, implement code, then make tests pass.
- For coverage-sensitive changes, run `./gradlew jvmJacocoTestReport` and include the `build/jacoco/jacoco.xml` or `build/jacocoHtml` output in PR notes if requested.

Workflows and conventions
- Commits: use concise imperative subject lines and include `#issue` where applicable.
- PRs: include a short summary, testing steps, and any cross-repo impacts (list `earth-app/crust`, `earth-app/sky`, `earth-app/cloud` if they will be touched).
- CI: rely on workflows in [.github/workflows](.github/workflows). If you need new steps (e.g., cross-repo integration tests), propose them in the PR description.

How to use `/init` and custom commands
- If you have access to Claude Code, run `/init` in a Claude session pointed at this repo to generate a starter CLAUDE.md. Review and prune autogenerated content.
- For repetitive prompts, add files into `.claude/commands/` (example: `performance-optimization.md`) and reference them with slash-commands.

Sensitive information
- Do not record secrets, tokens, or credentials in `CLAUDE.md` or any checked-in files. Mention only the environment variable names required for CI (e.g., `GITHUB_TOKEN`, `NPM_TOKEN`).

Notes for maintainers
- Keep this file concise — prefer links to detailed docs (e.g., API design docs, deployment runbooks).
- Update this file when you change release/publish processes, the CI matrix, or key repo integrations.

Suggested next steps
1. Copy or adapt this CLAUDE.md to the other repos that interact with `ocean`: `earth-app/crust`, `earth-app/sky`, and `earth-app/cloud`.
2. Optionally add `.claude/commands/performance-optimization.md` for common perf-review prompts.

If you want, I can:
- Add repository-specific CLAUDE.md stubs to `earth-app/crust`, `earth-app/sky`, and `earth-app/cloud` (requires access to those repos).
- Commit this file and open a PR.
- Run the test commands locally and report results.

----
Generated from guidance in: https://claude.com/blog/using-claude-md-files
